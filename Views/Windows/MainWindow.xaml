<Window x:Class="MaterialDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MaterialDemo"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Title="MainWindow" Height="700" Width="1200">
    <!-- WindowStyle="None" AllowsTransparency="True"  WindowState="Maximized"-->
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <Image Source="pack://application:,,,/Assets/background.png" Stretch="UniformToFill"/>
        <Border Grid.Column="0">
            <!--<Border.Background>-->
                <!--<ImageBrush ImageSource="pack://application:,,,/Assets/background.png"/>-->
                <!--<LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#3AA9AD"></GradientStop>
                    <GradientStop Color="#3AADA1"></GradientStop>
                </LinearGradientBrush>-->
            <!--</Border.Background>-->

            <Grid>
                <Canvas>
                    <!--<Polygon Points="0,20 330,170 0,350" Fill="DarkBlue" Opacity="0.2"></Polygon>-->
                    <!--<Polygon Points="100,400 200,370 180,470" Fill="#6EB1B6"></Polygon>-->
                </Canvas>

                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Sign Up" Style="{StaticResource titleText}"/>
                    <TextBlock Text="Entry you personal info or place Identification Card " Style="{StaticResource normalText}" Opacity="0.8" Margin="20 30"/>
                    <Button Content="Sign Up" Style="{StaticResource button}"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Column="1" Background="#ffffff" >
            <Grid Margin="0,30,0,0">

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="45 0 0 0" Orientation="Horizontal" VerticalAlignment="Center" IsHitTestVisible="False">
                    <Image Width="30" Height="30" Source="pack://application:,,,/Assets/icon.png" />
                    <TextBlock Margin="15,0,0,0" Text="Project M." VerticalAlignment="Center" FontWeight="SemiBold" FontSize="20" />
                </StackPanel>

                <StackPanel Grid.Row="1" VerticalAlignment="Center">
                    <TextBlock Text="登录" Style="{StaticResource titleText}" Foreground="#3AB19B"></TextBlock>
                    <StackPanel Orientation="Horizontal" Margin="0 20" HorizontalAlignment="Center">
                        <TextBlock Text="请输入登陆凭证:" Style="{StaticResource normalText}" Foreground="DimGray" Margin="0 10 0 15"></TextBlock>
                    </StackPanel>

                    <Border BorderThickness="1" BorderBrush="DimGray" Margin="70 7" CornerRadius="5">
                        <Grid Margin="7 9" >
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <materialDesign:PackIcon Kind="Abacus" />
                            <TextBlock x:Name="username" Text="Username" MouseDown="username_MouseDown" Style="{StaticResource textHint}"/>
                            <TextBox x:Name="usernameBox" TextChanged="usernameBox_TextChanged" Style="{StaticResource textBox}"/>
                        </Grid>
                    </Border>

                    <Border BorderThickness="1" BorderBrush="DimGray" Margin="70 7" CornerRadius="5">
                        <Grid Margin="7 9">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <materialDesign:PackIcon Kind="Abacus" />
                            <TextBlock x:Name="password" Text="Password" MouseDown="password_MouseDown" Style="{StaticResource textHint}"/>
                            <PasswordBox x:Name="passwordBox" PasswordChanged="passwordBox_PasswordChanged" Style="{StaticResource textBox}" />
                        </Grid>
                    </Border>

                    <Button Content="Sign In" Style="{StaticResource submitButton}"/>

                </StackPanel>
            </Grid>
            
        </Border>

        
    </Grid>
</Window>
